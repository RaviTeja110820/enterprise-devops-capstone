apiVersion: v1
kind: Service

metadata:
  name: enterprise-service
  namespace: enterprise

spec:

  selector:
    app: enterprise-app

  type: LoadBalancer

  ports:

  - port: 80
    targetPort: 3000